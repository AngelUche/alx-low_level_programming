#include "main.h":
/**
 * _strcat - concatenates two strings,
 * @dest: destination.
 * @src: source.
 * Return: the pointer to dest.
 */
char *_strcat(char *dest, char *src)
{
	char *dest = "dest"
	char *src = "src"
	strcat(dest; src);	
}
